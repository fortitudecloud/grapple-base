{
  "name": "mongo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "tsc -w -p ./src",
    "debug": "nodemon --watch ./dist --debug=0.0.0.0:5858 --debug-brk --nolazy",
    "nodemon": "nodemon --watch ./dist --nolazy",
    "start": "node ./dist/index.js",
    "sync": "nobone-sync -s"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/grapple": "file:../../types-grapple",
    "@types/reflect-metadata": "0.0.5",
    "nodemon": "^1.12.1"
  },
  "dependencies": {
    "grapple": "file:../../grapple",
    "grapple-json-db": "file:../../grapple-json-db",
    "reflect-metadata": "^0.1.10"
  }
}
